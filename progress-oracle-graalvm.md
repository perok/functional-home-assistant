# Landed — safe to delete

The Oracle GraalVM investigation is finished and written into
`docs/spike-compiled-truffle.md` (new section "Does Oracle GraalVM change any of this?",
plus corrections to the Pkl headline, the memory attribution, and a new section on the
polyglot/JDK version lockstep) and `docs/plan-history-view.md`.

Nothing here is still owed. This file survives only so the maintainer can delete it
deliberately; it holds no content the docs do not.
