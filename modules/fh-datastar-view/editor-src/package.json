{
  "name": "fh-editor-vendor",
  "private": true,
  "type": "module",
  "description": "Bundles CodeMirror 6 + @codemirror/lsp-client into a single self-contained vendor.js for the dashboard editor. Run `npm install && npm run build` when the deps change; the built vendor.js is committed so the app needs no CDN or import map.",
  "scripts": {
    "build": "esbuild vendor.entry.js --bundle --format=esm --outfile=../src/main/resources/editor/vendor.js"
  },
  "dependencies": {
    "@codemirror/autocomplete": "^6",
    "@codemirror/commands": "^6",
    "@codemirror/language": "^6",
    "@codemirror/lint": "^6",
    "@codemirror/lsp-client": "^6",
    "@codemirror/state": "^6",
    "@codemirror/view": "^6"
  },
  "devDependencies": {
    "esbuild": "^0.28"
  }
}
